# firmware-h2 — ESP32-H2 Zigbee coprocessor
# Top-level project CMake for the H2 firmware.
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(firmware-h2)
