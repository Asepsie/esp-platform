# firmware-c6 — ESP32-C6 primary MCU
# Top-level project CMake for the C6 firmware (BACnet, control, UI, OTA).
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(firmware-c6)
